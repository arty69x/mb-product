export default function WishlistButton() {
  return <button aria-label="Add to wishlist" className="rounded border border-borderLight bg-white px-2 py-1 text-xs hover:border-accent focus-visible:ring-2 focus-visible:ring-accent">Wishlist</button>;
}
